{
  "name": "viagem-missionaria",
  "version": "0.0.0",
  "private": true,
  "description": "Plataforma para captura e consulta de dados de viajantes de viagens missionárias (formulário web + app mobile offline).",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
