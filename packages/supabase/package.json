{
  "name": "@viagem/supabase",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(no linter configured for the monorepo yet)\"",
    "gen:types": "supabase gen types typescript --linked --schema public > src/database.types.ts"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
