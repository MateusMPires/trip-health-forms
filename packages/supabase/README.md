# packages/supabase — Acesso a dados

Client Supabase compartilhado, **tipos gerados** do banco e queries reutilizáveis.

- `src/` — factory do client, tipos gerados (`supabase gen types`) e helpers de query.

Nomenclatura do banco em **inglês / `snake_case`** (ver Fase 1). Consome tipos de domínio de
`packages/core` quando aplicável.
